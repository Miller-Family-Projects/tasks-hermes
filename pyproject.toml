[project]
name = "tasks-hermes"
version = "0.1.0"
description = "Hermes plugin for simple JSON-backed tasks with notes."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
authors = [
  { name = "tasks-hermes contributors" },
]
keywords = ["agent", "hermes", "notes", "plugin", "tasks"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Typing :: Typed",
]
dependencies = []

[project.urls]
Homepage = "https://github.com/Miller-Family-Projects/tasks-hermes"
Issues = "https://github.com/Miller-Family-Projects/tasks-hermes/issues"
Source = "https://github.com/Miller-Family-Projects/tasks-hermes"

[dependency-groups]
dev = [
  "basedpyright>=1.35.0",
  "pytest>=9.0.0",
  "ruff>=0.14.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["tasks_hermes"]

[tool.hatch.build.targets.wheel.force-include]
"tasks_hermes/plugin.yaml" = "tasks_hermes/plugin.yaml"
"tasks_hermes/py.typed" = "tasks_hermes/py.typed"

[tool.hatch.build.targets.sdist]
include = [
  "/LICENSE",
  "/README.md",
  "/pyproject.toml",
  "/tests",
  "/tasks_hermes",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["ALL"]
ignore = [
  "COM812",
  "D100",
  "D101",
  "D102",
  "D103",
  "D104",
]

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = ["S101", "D", "PLR2004", "SLF001"]

[tool.basedpyright]
typeCheckingMode = "all"
pythonVersion = "3.11"
include = ["tasks_hermes", "tests"]
